general:
  instance_name: "AutoDL SearXNG"

search:
  safe_search: 0
  formats:
    - html
    - json

server:
  port: 8080
  bind_address: "0.0.0.0"
  secret_key: "${SEARXNG_SECRET:-changeme}"
  limiter: false
  public_instance: false

ui:
  static_use_hash: true
