# Port for the nginx entry point
PORT=8080

# SearXNG URL (internal — used by the app inside Docker)
# Do not change unless you know what you're doing
SEARXNG_URL=http://searxng:8080

# Download directory
DOWNLOAD_DIR=./downloads

# Search defaults
MAX_RESULTS=20
DEFAULT_TYPE=pdf
